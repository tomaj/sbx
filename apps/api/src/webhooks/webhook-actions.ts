// Re-export from shared types package — single source of truth for webhook action strings.
export { WEBHOOK_ACTIONS, ALL_WEBHOOK_ACTIONS } from '@sbx/types';
export type { WebhookAction } from '@sbx/types';
